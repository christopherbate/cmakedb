cmake_minimum_required(VERSION 3.20)
project(provenance C)
option(BUILD_LEGACY_DRIVER "build the legacy driver" OFF)
option(ENABLE_PROFILING "enable profiling hooks" OFF)
option(ENABLE_LOGGING "verbose logs" ON)
include(cmake/FindDeps.cmake)
function(setup_prof)
  if(ENABLE_PROFILING)
    message(STATUS "profiling on")
  endif()
endfunction()
macro(set_common_flags)
  set(tmp_flags "-Wall")
  set(COMMON_FLAGS "${tmp_flags}")
endmacro()
set_common_flags()
if(ENABLE_LOGGING)
  add_compile_definitions(LOGGING=1)
endif()
set(STALE_VALUE "first")
set(STALE_VALUE "second")
message(STATUS "stale is ${STALE_VALUE}")
add_subdirectory(src/http)
add_subdirectory(src/net)
add_subdirectory(src/app)
set(_early_probe "${LATE_DECLARED_OPT}")
if(LATE_DECLARED_OPT)
endif()
option(LATE_DECLARED_OPT "declared after its first reference" ON)
set(_typo_probe "${ENABLE_LOGING}")
if("$<CONFIG:Debug>" STREQUAL "x")
endif()
set(_genex_ok "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/src/http/include>")
target_include_directories(http PRIVATE ${_genex_ok})
