cmake_minimum_required(VERSION 3.20)
project(multiconfig NONE)
option(FEATURE_X "read only when extras are on" OFF)
option(ALWAYS_DEAD "read in no configuration" OFF)
if(WITH_EXTRA_READS)
  if(FEATURE_X)
    message(STATUS "feature x enabled")
  endif()
endif()
