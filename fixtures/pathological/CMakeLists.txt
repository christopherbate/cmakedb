cmake_minimum_required(VERSION 3.20)
project(patho NONE)
set(A 1)
set(DOC [=[bracket ${NOT_REF} content]=])
set(ML "multi
line value")
set(GENEX "$<$<CONFIG:Debug>:dbg>")
foreach(i RANGE 2)
  set(LOOPED "${i}")
endforeach()
if(A AND DOC)
  set(BOTH 1)
endif()
message(STATUS "${A}${DOC}${ML}${GENEX}${LOOPED}${BOTH}")
